This program allows one to open a chatroom that hosts multiple clients on the same server using sockets. Usage is as follows 

java ConfServer "portnumber"

java ConfClient localhost "portnumber" "name"